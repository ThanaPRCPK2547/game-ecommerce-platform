# PHP Website สำหรับ Awardspace.net

เว็บไซต์ที่พัฒนาด้วย PHP และ MySQL พร้อมระบบจัดการข้อมูลครบครัน เหมาะสำหรับ deploy ลง Awardspace.net

## ✨ คุณสมบัติ

- 🏠 **หน้าแรก** - แสดงสถิติและข้อมูลสำคัญ
- 🛍️ **ระบบจัดการสินค้า** - เพิ่ม แก้ไข ลบ สินค้า
- 📰 **ระบบจัดการบทความ** - จัดการโพสต์และบทความ
- 🏷️ **ระบบจัดการหมวดหมู่** - จัดกลุ่มสินค้าและบทความ
- 👥 **ระบบจัดการผู้ใช้** - จัดการบัญชีผู้ใช้
- 🎨 **Responsive Design** - ใช้งานได้ทุกอุปกรณ์
- 🔐 **ระบบ Admin Panel** - จัดการเว็บไซต์อย่างง่ายดาย

## 🛠️ เทคโนโลยีที่ใช้

- **PHP 7.4+** - ภาษาหลัก
- **MySQL 5.7+** - ฐานข้อมูล
- **HTML5 & CSS3** - โครงสร้างและสไตล์
- **JavaScript (ES6+)** - ปฏิสัมพันธ์
- **Font Awesome** - ไอคอน
- **PDO** - การเชื่อมต่อฐานข้อมูลอย่างปลอดภัย

## 📁 โครงสร้างไฟล์

```
mini_project/
├── index.php          # หน้าเว็บหลัก
├── admin.php          # หน้า Admin Panel
├── config.php         # การตั้งค่าฐานข้อมูล
├── style.css          # ไฟล์ CSS
├── database.sql       # ไฟล์ SQL สำหรับสร้างฐานข้อมูล
├── .htaccess          # การตั้งค่า Apache
└── README.md          # คู่มือการใช้งาน
```

## 🚀 วิธีการ Deploy ลง Awardspace.net

### 1. เตรียมไฟล์
- อัปโหลดไฟล์ทั้งหมดลง Awardspace.net ผ่าน File Manager หรือ FTP
- วางไฟล์ในโฟลเดอร์ `public_html` หรือ `htdocs`

### 2. สร้างฐานข้อมูล
1. เข้า Awardspace Control Panel
2. ไปที่ **MySQL Databases**
3. สร้างฐานข้อมูลใหม่
4. สร้างผู้ใช้ฐานข้อมูลและกำหนดสิทธิ์
5. เปิด phpMyAdmin
6. Import ไฟล์ `database.sql`

### 3. ตั้งค่าการเชื่อมต่อฐานข้อมูล
แก้ไขไฟล์ `config.php`:

```php
define('DB_HOST', 'localhost');
define('DB_NAME', 'your_database_name');     // ชื่อฐานข้อมูลจาก Awardspace
define('DB_USER', 'your_username');          // ชื่อผู้ใช้จาก Awardspace
define('DB_PASS', 'your_password');          // รหัสผ่านจาก Awardspace
define('SITE_URL', 'https://yourdomain.awardspace.com');
```

### 4. ตั้งค่าสิทธิ์ไฟล์
- ตั้งค่าไฟล์ `.htaccess` ให้สามารถอ่านได้
- ตั้งค่าโฟลเดอร์ `uploads` (ถ้ามี) ให้สามารถเขียนได้ (755)

### 5. ทดสอบเว็บไซต์
- เข้าเว็บไซต์ผ่าน URL ที่ Awardspace ให้
- ทดสอบการเข้าสู่ระบบ Admin: `yourdomain.awardspace.com/admin.php`
- ข้อมูลเข้าสู่ระบบ: `admin` / `admin123`

## 🔧 การตั้งค่าเพิ่มเติม

### เปลี่ยนรหัสผ่าน Admin
1. เข้า phpMyAdmin
2. ไปที่ตาราง `users`
3. แก้ไขรหัสผ่านในฟิลด์ `password`
4. ใช้ฟังก์ชัน `password_hash()` ใน PHP เพื่อเข้ารหัส

### เปลี่ยนชื่อเว็บไซต์
แก้ไขในไฟล์ `config.php`:
```php
define('SITE_NAME', 'ชื่อเว็บไซต์ของคุณ');
```

### เพิ่มฟีเจอร์ใหม่
- ดูตัวอย่างการเพิ่มฟีเจอร์ใน `admin.php`
- ใช้ PDO สำหรับการเชื่อมต่อฐานข้อมูล
- ใช้ prepared statements เพื่อความปลอดภัย

## 📱 การใช้งาน

### สำหรับผู้เยี่ยมชม
- ดูสินค้าและบทความในหน้าแรก
- ใช้เมนูนำทางเพื่อดูข้อมูลต่างๆ
- ติดต่อผ่านหน้า Contact

### สำหรับ Admin
1. เข้า `admin.php`
2. เข้าสู่ระบบด้วย `admin` / `admin123`
3. จัดการสินค้า บทความ และหมวดหมู่
4. ดูสถิติการใช้งาน

## 🔒 ความปลอดภัย

- ใช้ PDO และ prepared statements
- ตรวจสอบข้อมูล input
- ใช้ session สำหรับการจัดการผู้ใช้
- ป้องกันการเข้าถึงไฟล์สำคัญผ่าน `.htaccess`

## 🐛 การแก้ไขปัญหา

### ไม่สามารถเชื่อมต่อฐานข้อมูล
- ตรวจสอบข้อมูลใน `config.php`
- ตรวจสอบว่า MySQL service ทำงานอยู่
- ตรวจสอบสิทธิ์ผู้ใช้ฐานข้อมูล

### หน้าเว็บไม่แสดงผล
- ตรวจสอบ PHP version (ต้อง 7.4+)
- ตรวจสอบ error log
- ตรวจสอบการตั้งค่า `.htaccess`

### ไม่สามารถเข้าสู่ระบบ Admin
- ตรวจสอบข้อมูลในตาราง `users`
- ลองรีเซ็ตรหัสผ่านผ่าน phpMyAdmin

## 📞 การสนับสนุน

หากมีปัญหาหรือคำถาม:
- ตรวจสอบเอกสารของ Awardspace.net
- ดู error log ใน Control Panel
- ติดต่อทีมสนับสนุนของ Awardspace

## 📄 License

MIT License - ใช้งานได้อย่างอิสระ

---

**หมายเหตุ:** ไฟล์นี้พร้อมใช้งานกับ Awardspace.net และ hosting provider อื่นๆ ที่รองรับ PHP และ MySQL
